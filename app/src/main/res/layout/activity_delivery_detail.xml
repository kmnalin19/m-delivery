<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:map="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        >
<fragment
          android:layout_width="match_parent"
          android:layout_height="match_parent"
          android:id="@+id/map"
          tools:context=".mvp.vp.delivery.DeliveryDetailActivity"
          android:name="com.google.android.gms.maps.SupportMapFragment"/>

        <support.fonts.MediumTextView
                android:id="@+id/delivery_list_row"
                android:layout_height="@dimen/list_image_height_weight"
                android:layout_width="match_parent"
                android:gravity="center"
                android:textSize="@dimen/small_text_size"
                android:textColor="@color/colorPrimaryDark"
                android:background="@color/background_color_white"
                android:layout_margin="@dimen/card_normal_margin"
                android:layout_alignParentBottom="true"/>

</RelativeLayout>
